export enum Paths {
  ROOT = "/",
  HOME = "/home",
  ABOUT = "/about",
  EVENTS = "/events",
}

export enum ApiEndpoints {
  HOME = "Home",
  NAVIGATION = "Navigation",
  HERO_BANNER = "Hero_Banner",
  EVENTS = "Events",
  CLIENTS = "Clients",
  FOOTER = "Footer",
}

export enum Cookies {
  TOKEN = "jwt",
  USERNAME = "username",
  LANGUAGE = "NEXT_LOCALE",
}

export enum DateFormats {
  SHORTHAND = "DD MMM YYYY",
}

export const defaultLanguage = "en-GB";
